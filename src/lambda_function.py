def handler(event, context):
    """Simple Lambda handler used as the function entrypoint.

    Replace or extend this with the actual Home Assistant / Alexa integration logic.
    """
    return {"statusCode": 200, "body": "Hello from ha-alexa-integration"}
