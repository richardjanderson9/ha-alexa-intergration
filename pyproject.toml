[project]
name = "ha-alexa-integration"
version = "0.1.0"
description = "Home Assistant Alexa integration lambda"
requires-python = ">=3.14"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
